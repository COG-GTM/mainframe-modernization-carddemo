package com.carddemo.context;

/** {@code CDEMO-PGM-CONTEXT} of copybook {@code COCOM01Y}. */
public enum ProgramContext {
    /** {@code CDEMO-PGM-ENTER}: first entry into the program. */
    ENTER,
    /** {@code CDEMO-PGM-REENTER}: the program is re-entered with a screen the user already saw. */
    REENTER
}
