package com.carddemo.batch.config;

import org.springframework.context.annotation.Configuration;

/**
 * DataSource configuration.
 * Spring Boot auto-configures the DataSource from application.yml properties.
 * This class is a placeholder for any future custom DataSource configuration
 * (e.g., connection pooling tuning, multiple data sources).
 */
@Configuration
public class DataSourceConfig {
    // Spring Boot auto-configuration handles DataSource setup via application.yml
}
